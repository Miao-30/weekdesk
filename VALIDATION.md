# 验证记录 · 1.2.0 · 2026-09-06

## 自动化与构建

- `npm.cmd test`：6/6 通过，无失败、跳过或取消。
- 公开演示测试覆盖单双周、不连续周次、北京时间周界、教师与地点按周变化、晚课、未排时间课程，以及待办校验、课程关联、重复和提醒。
- TypeScript 严格检查、Vite 生产构建、Electron 主进程和 preload 构建全部通过。
- Windows x64 NSIS 安装包构建成功，版本及 package-lock 根版本均为 1.2.0。

## 视觉与响应式检查

- 1280×720 与 820×600 的课表、待办页面均无横向溢出。
- 顶部标题栏为 34px，仅显示现有日历图标、课表/待办导航及窗口按钮，不再显示“周”字 Logo 或“周序”文字。
- 课表工具栏为 44–45px；820px 宽度下仍完整显示周次切换、非本周开关和同步按钮。
- 星期表头为 32px，只包含“周一”至“周日”；日期数字和“今天”文字已移除，今天列通过背景与底部强调线区分。
- 课程卡片圆角实测为 1px，间距、内边距和阴影均已压缩；浅灰背景与低饱和课程色在两种尺寸下可辨认。
- 10、12、14、18px 四档字号均实际切换检查；10px 和 18px 两个极端下没有横向溢出或卡片重叠。
- 10px、820×600 时可见至第 11 节附近，教师、教室、节次和非本周文字仍可辨认。
- 待办宽窗口使用 164px 左侧筛选栏；820px 时切换为约 49px 顶部横向筛选。任务使用细分隔线、方形勾选框和右侧截止时间。
- 快速新增、搜索、排序、标签、课程关联、优先级、完成和编辑入口仍正常显示。

## 数据与 Electron 兼容

- 未修改 Todo、课程或教务解析数据结构，也未改变 `appId`、用户数据目录和学校会话分区。
- 使用隔离用户目录启动正式 `win-unpacked` 构建，主进程及子进程正常运行。
- 导入旧版状态后成功恢复版本 1 课表、待办、最后打开的页面和字号偏好。
- 字号 IPC 校验范围已改为 10–18px；已有 12–18px 设置原样兼容。

## 安装包内容

- `app.asar` 包含生产前端、`dist-electron/main.cjs` 和 `dist-electron/preload.cjs`。
- 安装包不包含 `preview.json` 或个人课表预览数据。
- 安装包：`Weekdesk-Setup-1.2.0.exe`，大小 95,013,953 字节。
- SHA-256：`6D35DBCC77DFB9760D82A62D336E4F98CD72E6CD86F6FC341FC776DC9DCD73DA`。

## 实际使用环境说明

- 首次覆盖安装 1.2 后建议确认一次教务登录会话仍有效；本地数据兼容已经通过隔离状态启动验证。
- 物理睡眠/唤醒、Windows 通知权限、开机启动和动态 DPI 切换依赖本机系统状态，相应处理逻辑沿用 1.1。
- 学校尚未更新到“我的课表”页面的临时调课通知仍无法自动得知。
